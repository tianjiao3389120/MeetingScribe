name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-14
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Tests
        run: swift test
      - name: Release build
        run: swift build -c release
