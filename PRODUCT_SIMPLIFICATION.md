# MeetingScribe 产品简化方向

状态：讨论中。后续新增功能应优先遵守本文原则，避免主流程重新变复杂。

## 核心原则

- 主流程保持为：拖入文件 → 开始处理 → 复制纪要。
- 默认界面只展示高频操作，低频配置放入“更多选项”或“高级设置”。
- 正常状态不展示诊断信息；只在异常发生时解释技术细节。
- 同一个动作只保留一个名称和入口，例如自动保存进资料库后，对外文件操作统一称为“导出”。
- 不确定会长期维护的功能不进入一级界面。

## 已确认方向

### 准备页

默认只保留会议名称、项目、材料和“开始处理”。纪要模板跟随项目；本次背景和模板进入更多选项；标签改为生成后整理；新建项目入口保持轻量。

实施状态：已完成第一版。名称、项目和材料默认显示；模板、本次背景和标签已收入“更多选项”；窗口尺寸已缩小。

### 结果页

主要操作只保留复制、导出、生成邮件和“更多”。Markdown 源码、重新分离、声纹登记、反馈、用其他应用打开等进入“更多”。正常情况下隐藏语音段数、画面数和结构化状态。

实施状态：已完成第一版。正常状态只显示生成邮件、复制、导出和更多；仅在格式回退或保存异常时显示技术状态。

### 资料库

一级范围收敛为最近会议、待办、项目和归档。标签、收藏和排序进入筛选。详情默认只保留纪要、待办和逐字稿；邮件生成后再出现，信息进入详情按钮。历史待办回溯归入项目页面。

### 设置

常用设置只保留 AI 服务等跨会议配置；“本次识别场景”放在会议准备页，默认使用推荐值，临时选择不写回全局设置。说话人分离默认启用并自动判断实际发言人数，不再提供重复的总开关和会前人数设置。模型、画面密度、纪要指令、词表、声纹、存储、备份和诊断进入高级设置。固定 API 服务默认不暴露接口地址。

### 术语

“会议空间”简化为“项目”或“客户/项目”；“项目行动项台账”简化为“项目待办”；“项目更新建议”简化为“待确认更新”。

### 遗留功能

若确认不再恢复实时字幕，应删除完整实现、Helper、历史管理和存储入口，而不是继续维护隐藏功能。评估删除 Markdown 源码切换、独立反馈表单、处理前标签和重复导出入口。

## 首页优化

当前空闲页的拖放区会随窗口无限放大，导致大面积空白。建议：

1. 默认窗口约 760 × 500，导入卡片最大宽度约 560，不随窗口无限扩张。
2. 主按钮统一为“导入会议”，拖放只是同一区域的快捷方式；“录制会议”作为次要按钮。
3. 有历史数据时，在导入卡片下方显示最近 3 场会议，只展示名称、时间和一个打开入口。
4. 没有历史数据时显示三步首次使用引导：配置 AI、下载识别模型、导入会议；完成后不再展示。
5. 底部只显示有用的就绪状态或错误，例如“Codex CLI 已就绪”；删除泛化的技术宣传文字。
6. 历史和设置保留在工具栏，不把首页扩展成完整仪表盘。

实施状态：已完成第一版，包括紧凑默认窗口、主次操作、最近三场会议和模型就绪状态。

## 推荐实施顺序

1. 首页与准备页。
2. 结果页操作收敛。
3. 设置分层。
4. 资料库信息架构。
5. 删除确认不再维护的实时字幕遗留代码。
